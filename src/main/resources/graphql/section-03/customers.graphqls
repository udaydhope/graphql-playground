type NewCustomer {
    id : ID
    name: String
    age: Int
    city: String
    #orders : [NewCustomerOrder]! # if you see this this custoemr has list of order by in our java class it need not be as like this
    orders(limit: Int!): [NewCustomerOrder]! ## its just that we adding one limit parameter
}

type NewCustomerOrder {
    id: ID
    description: String
}

extend type Query {
    getAllNewCustomers: [NewCustomer!]

}